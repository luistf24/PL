Principal
{
    procedimiento A(int a, double B, bool c[][])
    {
    	var
    		double j, u, i;
    		int x, y, z;
    		char l;
    		bool d;

    		int n[5];
    		double m[2, 10];
    		char k[2,8];
    		bool d[z], g[1,2];
    	endvar

    	procedimiento B(int a, int b)
    	{
            
    		procedimiento C()
    		{
    			x = 10;

    			si (x > 10 && y <= 4) entonces
    			{
    				A(x,j,g);
    				y = {1, 2, 5, 4};
    			}
    			
    			sino
    				si ((x + 10) > y + 4) entonces
                        y = y + 2;
    		}

    	}
    }

    procedimiento D()
    {
    	procedimiento E()
    	{
			var 
				int vector[10];
				int g, u;
			endvar

            Salida "Resultado : ", 10;

            si(vector[vector[0]] == 100) entonces
            {
                vector[g] = 6;
            } sino { u = 0; }
    	}
    }
}
